#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'tictactoe'

game = TicTacToe::Game.new(STDOUT)
game.start
game.set_game('x')
# while guess = gets.chomp
#   game.guess(guess)
# end
